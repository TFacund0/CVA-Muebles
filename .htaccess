# Disable directory browsing
Options -Indexes

<IfModule mod_rewrite.c>
	RewriteEngine On

	# Redirect everything into the public/ folder, our front-controller root.
	RewriteCond %{REQUEST_URI} !^/public/
	RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Disable server signature
ServerSignature Off
